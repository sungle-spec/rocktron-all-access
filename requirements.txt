# Pinned for reproducible installs. Bump deliberately, retest with
# `python tools/uat_headless.py reference/baseline-pre-RE.syx` (23/23).
Flask==3.1.3
Flask-SocketIO==5.6.1
python-rtmidi==1.5.8
simple-websocket==1.1.0

# Transitive pins (locked so a Flask/SocketIO point release can't drag in
# something incompatible without us noticing).
Werkzeug==3.1.8
python-engineio==4.13.1
python-socketio==5.16.1
bidict==0.23.1
